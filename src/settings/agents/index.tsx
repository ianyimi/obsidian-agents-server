import ObsidianAgentsServer from "~/index";

export default function AgentsSettings({ plugin }: { plugin: ObsidianAgentsServer }) {
	return (
		<p className="text-center w-full border border-red-400">Agents Settings</p>
	)

}
